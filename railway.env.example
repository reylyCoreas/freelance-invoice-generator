# Railway Deployment Environment Variables
# Copy these to your Railway project's environment variables section

# Required: Your business information
BUSINESS_NAME=Your Business Name
BUSINESS_EMAIL=your-email@example.com  
BUSINESS_ADDRESS=123 Business St, City, State 12345

# Application settings
NODE_ENV=production
PORT=3000
TAX_RATE=0.08
CURRENCY=USD
DEFAULT_EXPORT_FORMAT=PDF

# CORS settings (Railway will provide your domain)
ALLOWED_ORIGINS=https://your-app.railway.app

# Email settings (optional - configure with your email provider)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# JWT settings (generate a secure secret)
JWT_SECRET=generate-a-secure-random-string-for-production
JWT_EXPIRES_IN=7d

# Database (Railway will auto-provide this in production)
# DATABASE_URL is automatically set by Railway PostgreSQL service
# No need to manually configure DATABASE_URL on Railway

# Additional settings
LOG_LEVEL=info
MAX_PDF_AGE_HOURS=24
ENABLE_CORS=true
